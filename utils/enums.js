export const STATES = Object.freeze({
	LOADING: "LOADING",
	ERROR: "ERROR",
	IDLE: "IDLE",
	SUCCESS: "SUCCESS",
});

export const STATUSES = Object.freeze({
	TRUE: true,
	FALSE: false,
});
